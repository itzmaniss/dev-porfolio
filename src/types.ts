export interface Project {
    name: string;
    desc: string;
    repo: string;
  }
  
  export interface ExperienceDetails {
    start_date: string;
    end_date: string;
    experience: string[];
  }